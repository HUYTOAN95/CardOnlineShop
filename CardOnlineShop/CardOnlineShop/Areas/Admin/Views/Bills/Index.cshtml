@model IEnumerable<CardOnlineShop.Areas.Admin.Models.DataModel.Bill>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section header{
    Hóa Đơn
}

<div class="table-responsive table--no-card m-b-30">
    @using (Html.BeginForm("Index", "Bills", FormMethod.Get))
    {
        <div class="row form-group" style="margin-top:1%">
            <div class="col-lg-12">
                <div class="col-md-4">
                    <input type="text" value="@ViewBag.SearchString" class="form-control" name="searchString" />
                </div>
                <div class="col-md-4" style="margin-top:1%">
                    <button type="submit" class="btn btn-success">Search</button>
                    <a class="btn btn-primary" href="/Admin/Bills/Create">Add new</a>
                    <a class="btn btn-success" href="/Admin/Bills/Report">Report</a>
                </div>
            </div>
        </div>
    }
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Employee.EmployeeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreateDate)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Employee.EmployeeName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreateDate)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.BillID }) |
                    @Html.ActionLink("Details", "Details", new { id = item.BillID }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.BillID })
                </td>
            </tr>
        }

    </table>
</div>
